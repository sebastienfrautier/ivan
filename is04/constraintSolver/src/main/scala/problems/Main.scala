package problems

import com.contraintsolver.ConstraintNet
import constraints.ConstraintsImpl.Constraint
import constraints.Variable
import constraintsolver.ConstraintSolverAlgs



object Main {
  def main(args: Array[String]): Unit = {


  }

}



    /*

     val n = 5

    val RANGE = 0 to n
    val Array(brit, swede, dane, norwegian, german) = RANGE.toArray
    val Array(dogs, fish, birds, cats, horses) = RANGE.toArray
    val Array(tea, coffee, milk, beer, water) = RANGE.toArray
    val Array(pall_mall, dunhill, blends, blue_master, prince) = RANGE.toArray
    val Array(red, green, white, yellow, blue) = RANGE.toArray


    val nationality =  Variable("natinality",1,Array.fill(n)(RANGE.toArray)()
    val animal =  Variable("animal",2,Array.fill(n)(RANGE.toArray))
    val drink = Variable("drink",3,Array.fill(n)(RANGE.toArray))
    val color =  Variable("color",4,Array.fill(n)(RANGE.toArray))
    val smoke =  Variable("smoke",5,Array.fill(n)(RANGE.toArray))


    // All Different

    val allDifferent1 = Different(nationality,animal)
    val allDifferent2 = Different(nationality,drink)
    val allDifferent3 = Different(nationality,color)
    val allDifferent4 = Different(nationality,smoke)


    val allDifferent5 = Different(drink,color)
    val allDifferent6 = Different(drink,smoke)
    val allDifferent7 = Different(drink,animal)

    val allDifferent8 = Different(color,smoke)
    val allDifferent9 = Different(color,animal)

    val allDifferent10 = Different(smoke,animal)


    constraintNet.addVariable(nationality)




    // unär innerhalb einer variable
    // binär zwischen variablen

    constraintNet.addVariable(nationality)
    constraintNet.addVariable(animal)
    constraintNet.addVariable(drink)
    constraintNet.addVariable(color)
    constraintNet.addVariable(smoke)
*/





